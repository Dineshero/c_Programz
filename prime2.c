#include<stdio.h>
void main()
{
int low,high,i,fl;
printf("The prime numbers series");
printf("Enter the starting and ending values");
scanf("%d\n%d\n",&low,&high);
if(low<high)
{
fl=0;
for(i=2;i<low/2;++i)
{
if(low%i==0)
{
fl=1;
break;
}}
if(flag==0)
printf("%d",low);
++low
}
}
